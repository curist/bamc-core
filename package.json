{
  "name": "bamc-core",
  "version": "0.1.4",
  "description": "The core lib for BAMC",
  "main": "dist/main.js",
  "scripts": {
    "start": "poi",
    "dev": "poi",
    "build": "poi build src/index.js --source-map false --format cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "curist",
  "dependencies": {
    "debug": "^3.1.0",
    "net": "^1.0.2",
    "readable-stream": "^2.2.2",
    "telnet-stream": "^1.0.4"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "poi": "^10.2.10"
  }
}
